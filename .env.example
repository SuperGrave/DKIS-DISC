# =============================================================================
# コピーして使う: このファイルをコピーし、名前を .env にしてから値を埋めてください。
#   PowerShell:  copy .env.example .env
# =============================================================================

# --- Webhook / main.py / 本番では必須。dev_console.py だけ試すときは空のままでよい ---
LINE_CHANNEL_SECRET=
LINE_CHANNEL_ACCESS_TOKEN=

# ワーカー起動時に Push で一言（任意）。
# - LINE_BOOT_GREETING_USER_IDS=Ua...,Ub... …明示のみで送る
# - 省略でも OK … Supabase の known_line_users（過去に話しかけた userId を自動記録）から最新順で送る
# LINE_BOOT_GREETING_USER_IDS=
# LINE_BOOT_GREETING_PUSH_STORE_LIMIT=50
# LINE_BOOT_GREETING_SKIP_STORED_IDS=0

# --- 必須（開発コンソールもここだけあれば起動する）---
# OpenAI → API keys（ https://platform.openai.com/api-keys など）
OPENAI_API_KEY=

# --- 任意 ---
# 設定 JSON を別パスにしたいときだけ（未設定ならリポジトリ内 dist/settings.json）
# DKIS_SETTINGS_PATH=C:\\path\\to\\settings.json

# SEARCH コマンド用（未設定なら検索は案内のみになります）
GOOGLE_API_KEY=
GOOGLE_CX=

# --- Supabase（記憶ファイル・設定の永続化。未設定なら記憶コマンドはエラー文言のみ）---
# Project Settings → API（Project URL と service_role の secret）
SUPABASE_URL=
SUPABASE_KEY=

# ローカル Flask のポート（Render 等ではホストが自動設定）
PORT=5000
