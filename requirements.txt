# DKIS 依存パッケージ
# pip install -r requirements.txt で一括インストール
# （pyproject.toml を使用している場合は uv sync または pip install -e . を推奨）

flask>=3.0.0
flask-cors>=4.0.0
openai>=1.0.0
requests>=2.31.0
trafilatura>=2.0.0
chardet>=5.0.0
